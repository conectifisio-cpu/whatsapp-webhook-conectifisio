flask
requests
3.  **Crie o arquivo `vercel.json` (O Segredo):** Para evitar que a Vercel se confunda com o erro de "uv lock", crie um novo arquivo na pasta principal do GitHub chamado `vercel.json` com este conteúdo:
```json
{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/whatsapp.py" }
  ]
}
